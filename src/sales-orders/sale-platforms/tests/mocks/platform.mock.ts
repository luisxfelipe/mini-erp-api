import { SalePlatform } from '../../entities/sale-platform.entity';

export const platformMock: SalePlatform = {
  id: 1,
  name: 'Platform Mock',
  createdAt: new Date(),
  updatedAt: new Date(),
};
