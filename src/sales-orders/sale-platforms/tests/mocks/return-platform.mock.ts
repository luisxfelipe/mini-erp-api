import { ReturnSalePlatformDto } from '../../dto/return-sale-platform.dto';
import { platformMock } from './platform.mock';

export const returnPlatformMock: ReturnSalePlatformDto = {
  id: platformMock.id,
  name: platformMock.name,
};
