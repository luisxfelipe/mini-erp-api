import { ReturnPlatformDto } from '../../dto/return-platform.dto';
import { platformMock } from './platform.mock';

export const returnPlatformMock: ReturnPlatformDto = {
  id: platformMock.id,
  name: platformMock.name,
};
